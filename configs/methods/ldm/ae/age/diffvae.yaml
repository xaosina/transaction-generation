model:
  autoencoder:
    name: VAE
    params:
      use_time: True
      d_token: 40
      num_layers: 4
      n_head: 1
      factor: 5
    checkpoint: /workspace/transactionsv2/transaction-generation/log/generation/age/ready/vae/diff_vae.ckpt
    pretrain: True
    frozen: True
    batch_transforms:
      - TimeToDiff:
          disable: False