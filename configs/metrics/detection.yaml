evaluator:
  devices: ["cuda:1", "cuda:2"]
  topk: 1
  temperature: 1.
  metrics:
    - OTD
    - Cardinality
    - MultisetF1Metric:
        average: macro
    - MultisetF1Metric:
        average: micro
    - Detection:
        condition_len: ${data_conf.generation_len}
        verbose: True
    # - Density:
    #     log_cols: ["amount_rur"]
    #     with_timediff: True
    #     save_details: True
    #     verbose: False