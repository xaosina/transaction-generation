config_factory: [datasets/age/age,methods/gru,datasets/age/metrics/default]

# Runner params:
log_dir: log/generation
device: "cuda:0"
common_seed: 0

runner:
  name: GenerationTrainer
  run_type: simple
  seed_keys:
    - "common_seed"
  params:
    n_runs: 1
    n_workers: 1
  device_list:

logging:
  file_lvl: "info"
  cons_lvl: "info"
